# Projet MultiAgentIA - Dépendances minimales

# Appels réseau, scraping ou API
requests>=2.25

# Pour les éventuels appels LLM (à activer uniquement si tu utilises OpenAI)
openai>=0.27  # optionnel, à commenter si non utilisé

# Pour les tests unitaires (optionnel mais conseillé)
pytest>=7.0

# Pour garantir la compatibilité avec les versions de Python
python>=3.9
